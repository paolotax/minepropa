$("#appunti").html("<%= escape_javascript(render("appunti/appunti")) %>");
<!-- document.title = "<%= escape_javascript("#{params[:search].to_s.titleize} Appunti per #{(params[:sort] || 'name').titleize} - Page #{params[:page] || 1}") %>"; -->