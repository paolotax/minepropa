- title "Calendario"

#appunti.left_appunti
  #da_draggare
    = render :partial => 'appunti/appunto', :collection => @appunti

#appunti.right_appunti
  #calendar
      
  / #recent_episodes
  /     %h3
  /       Recent Railscasts Episodes
  /     %ul
  /       - for entry in FeedEntry.all(:limit => 10, :order => "published_at desc")
  /         %li= link_to h(entry.name), entry.url