$("#comment-notice").html('<div class="flash notice"><%= escape_javascript(flash.delete(:notice)) %></div>');


/* Add the new comment to the bottom of the comments list */
$("<%= escape_javascript(render(@appunto_riga)) %>").insertBefore("#td_notes_activities_list li:first");

/* Highlight the new comment */
$("#appunto_riga_<%= @appunto_riga.id %>").effect("highlight", {}, 3000);
$(".best_in_place").best_in_place();

/* Reset the comment form */
// $("#new_appunto")[0].reset();
// $(".token-input-token-facebook").remove();
// $('.token-input-list-facebook').hide();
// $('#task_create_input').show();
// $('.create_task_more').hide();