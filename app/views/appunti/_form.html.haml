- content_for :head do
  = formtastic_stylesheet_link_tag


#create-appunto
  = form_for @appunto, { :method => 'post',  :action => appunti_path, :remote => true } do |f|
    #create-task-input.first
      .inner
        .input
          %img#task_create_loader.class{ :src => "/images/ajax-loader.gif",  :alt => "loader", :style => "display:none;" }
          = f.text_field :destinatario, {:class => 'itext small', :placeholder => "... chi?"}
          = f.autocomplete_field :nome_scuola, autocomplete_scuola_nome_scuola_appunti_path, {:size => 40, :placeholder => "... dove?", :class => 'itext small'}
    = f.submit "Aggiungi", :class => "btn_green"
    / %a#task_create_btn_add.add.btn_green Aggiungi
    .clear
    .create_task_more
      #create-task-input
        .inner
          .input
            = f.text_field :note, { :class => 'itext', :placeholder => "... che cosa?"}
      #create-task-input.tags
        .inner
          .input
            %input#task_create_input.itext{ :name => "title", :placeholder => "... tagga"}
            = f.text_field :tag_tokens, {:class => 'itext small', :style => "display: none;"}
      #create-task-input.tags
        .inner
          .input
            = f.text_field :telefono, {:class => 'itext small', :placeholder => "... telefono"}
            = f.text_field :email,    {:class => 'itext small', :placeholder => "... email"}

      %ul#task_create_actions.actions 
        %li#create_task_action_reset.action.reset 
          %a
            %span#task_create_reset
              Resetta
      .clear
    
      = render :partial => 'appunti/form_vacanze'


/ .form_appunto
/   = semantic_form_for @appunto do |f|
/     - render 'shared/error_messages', :object => f.object
/   
/     = f.buttons do
/       = f.commit_button "Salva Modifiche", :button_html => { :class => 'btn_blue' }
/     = f.inputs do
/       = f.input :stato, :as => :radio, :collection => {"da consegnare" => "", "in sospeso" => "P", "consegnato" => "X" }
/       = f.input :destinatario, :input_html => { :size => 40  }, :wrapper_html => { :class => "important" } 
/       = f.autocompleted_input  :nome_scuola, :url => autocomplete_scuola_nome_scuola_appunti_path, :size => 40
/       = f.input :note,         :input_html => { :rows => 5, :cols => 40  }
/       %li.text.optional
/         = f.label :tag_tokens, "Tags"
/         = f.text_field :tag_tokens, "data-pre" => @appunto.tags.map(&:attributes).to_json
/       = f.input :telefono,     :input_html => { :size => 40  }
/       = f.input :email,        :input_html => { :size => 40  }
/ 
/ 
/     = f.semantic_fields_for :indirizzi do |indirizzo|
/       = indirizzo.inputs do
/         = indirizzo.input :destinatario
/         = indirizzo.input :indirizzo
/         = indirizzo.input :citta
/         = indirizzo.input :cap
/         = indirizzo.input :provincia
/         / = indirizzo.input :latitude
/         / = indirizzo.input :longitude
      