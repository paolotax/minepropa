- if object.errors.any?
  #error_explanation
    %h2
      = pluralize(object.errors.count, "error")
      impossibile salvare questo 
      = object.class.to_s.underscore.humanize
    %p
      ci sono problemi con i seguenti campi:
      %ul
        = object.errors.full_messages.each do |msg|
          %li=msg